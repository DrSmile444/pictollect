// convert this field into enum
// step: 'drive' | 'directory' | 'date' | 'name' | 'progress';
export enum PhotoStep {
  DRIVE = 'drive',
  DIRECTORY = 'directory',
  DATE = 'date',
  NAME = 'name',
  PROGRESS = 'progress',
}
